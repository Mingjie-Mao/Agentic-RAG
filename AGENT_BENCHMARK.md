# Agent Benchmark、Memory A/B 与训练决策

- 运行日期：2026-09-17 至 2026-09-18
- 开发回归任务：[`fixtures/agent/tasks.json`](./fixtures/agent/tasks.json)
- 最终结果：[`artifacts/b2-agent-benchmark-final.json`](./artifacts/b2-agent-benchmark-final.json)
- 首轮三路结果：[`artifacts/b1-agent-benchmark.json`](./artifacts/b1-agent-benchmark.json)
- 失败语料：[`artifacts/agent-failure-corpus.json`](./artifacts/agent-failure-corpus.json)
- Memory A/B：[`artifacts/memory-ab-v1.json`](./artifacts/memory-ab-v1.json)

## 结论

**当前不训练，也不做 RL。** 修复证据覆盖、版本路由、复合问题验收和确定性冲突兜底后，
单次 RAG 与固定工作流在 30 道开发回归任务上均为 **30/30**，越权标记泄漏均为 0。
固定工作流没有显示出质量增益，中位延迟也与单次 RAG 基本相同，因此线上默认采用自动路由：
普通问题走单次 RAG，明确需要 Agent 任务状态时进入固定工作流，动态策略保留为实验模式。
公开访客后端进一步禁用动态模式，避免一次任务连续调用本地 7B policy 造成 142–304 秒等待。

| 策略 | 数据集 | 正确 | 准确率 | 中位延迟 | 总耗时 | 平均步骤 | 禁区泄漏 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 单次 RAG | B2 开发回归 30 题 | 30/30 | 100% | 22.0 s | 761.0 s | 1.0 | 0 |
| 固定工作流 | B2 开发回归 30 题 | 30/30 | 100% | 21.6 s | 809.2 s | 2.0 | 0 |
| 动态 Agent | B1 首轮开发 5 题 | 4/5 | 80% | 157.4 s | 836.9 s | 2.4 | 0 |

B2 覆盖事实、复合问答、冲突、ACL、跨租户、时间版本、表格和拒答八类任务。每类在两条
主路径上都通过，但单类只有 1–11 题；任务又参与了本轮故障定位与修复，因此 100% 代表开发
回归通过，不是独立留出集成绩。动态行来自修复前的 B1 小集合，只用于说明动态策略的成本，
不能与 B2 的 30 题准确率作同分母比较。

这 30 题中 fact 11 道、compound 10 道，绝大多数不要求“先观察结果，再决定下一步”。因此它们
正式定位为 **Basic Regression**：适合证明 RAG、ACL、引用和基础工作流没有回归，不足以判断
动态 Agent 的规划、恢复和停止能力。独立于它的 30 题 Hard Agent Benchmark v2 已完成分层运行且不污染 S3 冻结集。共享 21 题 Task Success：RAG 7/21、workflow 14/21、dynamic 7/21；动态 P50 166.2 秒、P95 253.4 秒。受控 9 题 workflow 与 dynamic 均为 4/9，安全泄漏为 0，Recovery Rate 为 0。设计和逐题结果见 `AGENT_HARD_BENCHMARK.md` 与 `artifacts/agent-hard-benchmark-v2.json`。

修复后另跑了 A04、A22、A24 三道动态困难门禁：跨文档冲突与精确时间冲突在
[`b2-agent-dynamic-hard.json`](./artifacts/b2-agent-dynamic-hard.json) 中通过；其中 A22 首次仍漏答，
加入结束前复合验收覆盖检索后在
[`b2-agent-dynamic-a22-final.json`](./artifacts/b2-agent-dynamic-a22-final.json) 通过。三题最终相关运行
均正确且无泄漏，耗时分别为 201.2、304.0、142.3 秒。这个定向门禁验证了修复，但不是完整
30 题动态 benchmark。

## 长期记忆开关 A/B

同一组 30 道冻结 Agent 开发任务使用固定 workflow、`temperature=0`、`seed=42` 与相同证据链，
只切换 `use_memory`；奇偶题交替先跑开／关，降低模型热身顺序偏差。三个虚构用户预先写入同一组
回答偏好，主指标仍是企业事实、状态和禁区词，记忆不能充当企业证据。

| 条件 | 正确 | 状态正确 | 禁区泄漏 | 中位延迟 | 平均 prompt token |
| --- | ---: | ---: | ---: | ---: | ---: |
| 关闭长期记忆 | 30/30 | 30/30 | 0 | 15.9 s | 1247.0 |
| 开启长期记忆 | 29/30 | 29/30 | 0 | 21.9 s | 1675.4 |

配对差值为 −3.3 个百分点，只有 A26 改变，McNemar 精确检验 `p=1.0`；样本不足以证明记忆造成
真实质量变化，更不支持“长期记忆提高企业事实正确率”。开启记忆平均增加 428.4 个 prompt token，
中位延迟增加 6.0 秒。A26 的审计结果是：问题明确询问另一租户，检索虽然没有越权，却把本租户
同主题资料交给模型，模型把星桥标记错归给海川。修复为检索前确定性识别外租户名称后，A26 的
开／关定向回归均正确，且不再调用生成模型；结果见
[`memory-ab-a26-foreign-tenant.json`](./artifacts/memory-ab-a26-foreign-tenant.json)。

因此长期记忆当前的收益定位仍是跨会话偏好与上下文复用，不是企业知识扩充。后续若要声称它
提高体验，需要另外预注册偏好遵从指标；事实 A/B 只承担“不降低正确率和安全性”的门禁。
当前实现已将它改为任务级显式开启，默认关闭，并将注入上下文限制为最多 3 条、单条 240 字、
总计 600 字。

## 本轮解决的正确率问题

1. **跨文档证据被单份资料占满**：复合／冲突问题扩大候选池，每份文档最多先取两段，过滤
   只有演示声明的样板片段。
2. **动态策略错误进入历史版本**：只有问题明确要求版本、历史或变更时才允许
   `compare_versions`；最终证据优先活动版本并按文档轮转。
3. **模型看见冲突却不输出精确值**：对可确定解析的时间区间，服务端要求两份不同文档与
   两个原始区间同时存在，模型漏写时使用可审计的确定性结论。
4. **复合问题漏答一半**：从明显的“分别／以及／同时”结构生成验收项，生成与结束判断都检查
   每个子问题。
5. **短事实题上下文过窄**：短问题候选数从 4 调到 6，再由权限和上下文预算收敛。
6. **评测器误判中文数字**：评分前把“百分之二”等形式归一化，并保留原始回答供审计。

## 为什么现在不训练

当前没有剩余的、可重复的 policy 选择错误。工程修复已经使两条低成本路径通过开发回归，
而修复前动态策略在 5 题上没有质量增益，中位延迟约为当时单次 RAG 的 4.7 倍。此时生成
rollout 做 SFT 或 RL，主要会学习开发题和现有控制器行为，无法证明对新任务有收益。

`scripts/collect_agent_failures.py` 从实际本地 benchmark 轨迹中去重出 **17 类历史失败**：
5 类冲突漏判、1 类错误作答、2 类错误拒答、9 类回答漏项。它们在后续 artifact 中均有相同 task/arm 的成功
回归，目前未解决样本为 0。这个数量和分布不足以训练；先持续积累失败轨迹，并对每条做人工
归因，避免让模型学习已经能用规则解决的问题。

`agent-v1` 不再追加独立 Agent 留出集：该项目目标是可供面试试用的工程交付，30 题分类回归、
权限硬回归和逐题 artifact 已足以支持当前功能冻结。限制仍应如实表述为“开发回归通过”，不能把
100% 外推到任意新任务。只有未来重新启动后训练研究时，才需要在训练前冻结一组从未参与调试的
评测任务。

未来训练的启动条件：

1. 在线试用收集至少 50 条经人工确认、可重放的失败轨迹，并归因为动作选择、停止时机或
   工具参数，而不是检索、提示词或评测器缺陷。
2. 在训练开始前冻结未参与数据生产和调参的评测任务；先做规则／workflow 基线，再做
   1.5B–3B LoRA/SFT。只有 SFT 在该评测上有净收益且仍有
   可验证的策略缺口，才开展一次设有预算上限的 RL 对照。
3. 训练实验必须报告质量、泄漏、延迟、token、峰值内存和失败恢复；任何安全回归都停止晋级。

## 资源判断

本轮全部使用本机 Qwen2.5 7B 与 Ollama，API 费用为 0。30 题两条主路径累计约 26 分钟模型
时间；动态策略按首轮速度估算会显著增加等待时间和能耗。面试项目当前最有价值的下一步是
真实试用反馈，而不是购买云 GPU。若未来达到训练门槛，先以 1.5B–3B LoRA
做 20–50 step 冒烟，再根据独立评测决定是否为 7B+ SFT 或 RL 购买限额云算力。
